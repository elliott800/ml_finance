# Contributing

This project contains research and prototype trading code. Please follow the
safety guidelines below when contributing.

Local setup

1. Create a virtual environment (Python 3.10+ recommended):

Running the project safely

- Default behavior is `--dry-run`. Always run `python scalper.py --dry-run` and
  verify logs before attempting live mode.

Validation checks

- Basic quick checks:

Reporting issues

- Open an issue describing the bug or proposed change and reference the relevant
  docs under `docs/improvements/`.

Code style

- Follow the repository linters and formatting rules (we recommend `black`,
  `ruff`, `isort`).
